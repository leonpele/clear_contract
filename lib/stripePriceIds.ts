/**
 * Optional fallback if STRIPE_PRICE_* / NEXT_PUBLIC_STRIPE_PRICE_* env vars are unset.
 * Paste your Stripe Price IDs (must start with price_, not prod_).
 */
export const STRIPE_FALLBACK_PRICE_ONETIME = '';
export const STRIPE_FALLBACK_PRICE_SUBSCRIPTION = '';
